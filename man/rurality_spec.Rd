% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{rurality_spec}
\alias{rurality_spec}
\title{Specification Curve Analysis Across Rurality Classification Schemes}
\usage{
rurality_spec(
  data,
  outcome,
  fips_col = "fips",
  covariates = NULL,
  schemes = c("rucc", "ruca", "nchs", "omb"),
  forms = c("ordinal", "binary"),
  covar_sets = c("minimal", "full", "state_fe"),
  plot = TRUE
)
}
\arguments{
\item{data}{A data frame containing a 5-digit county FIPS column and at
least one numeric outcome variable.}

\item{outcome}{A character vector naming one or more outcome columns in
\code{data}.}

\item{fips_col}{Name of the FIPS column in \code{data}. Default \code{"fips"}.}

\item{covariates}{Optional character vector of additional covariate column
names already present in \code{data}. These are added on top of the three
built-in covariate sets (see Details). Default \code{NULL}.}

\item{schemes}{Character vector of schemes to include. Any subset of
\code{c("rucc", "ruca", "nchs", "omb")}. Default: all four.}

\item{forms}{Character vector of functional forms. Any subset of
\code{c("ordinal", "binary")}. Default: both.}

\item{covar_sets}{Character vector of built-in covariate sets to use.
Any subset of \code{c("minimal", "full", "state_fe")}. Default: all three.
See Details.}

\item{plot}{Logical. If \code{TRUE} (default), print a specification curve
plot. Requires \pkg{ggplot2}.}
}
\value{
A tibble with one row per specification per outcome, containing:
\describe{
\item{outcome}{Outcome variable name}
\item{scheme}{Rurality scheme (\code{rucc}, \code{ruca}, \code{nchs}, \code{omb})}
\item{form}{Functional form (\code{ordinal} or \code{binary})}
\item{covars}{Covariate set label}
\item{n}{Number of observations}
\item{estimate}{Coefficient on the rurality predictor}
\item{std.error}{Standard error}
\item{statistic}{t-statistic}
\item{p.value}{p-value}
\item{conf.low}{Lower 95\% confidence interval}
\item{conf.high}{Upper 95\% confidence interval}
\item{r.squared}{Model R-squared}
}
}
\description{
Runs a specification curve across four rurality classification schemes
(RUCC, RUCA, NCHS, OMB), two functional forms (ordinal and binary
metro/nonmetro), and three covariate sets, for one or more user-supplied
county-level outcome variables. Returns a tidy data frame of coefficient
estimates suitable for plotting or further analysis.
}
\details{
\code{rurality_spec()} joins the package's \code{county_crosswalk} dataset (FIPS
backbone with RUCC, RUCA, NCHS, OMB, and ACS covariates) onto \code{data} by
FIPS code, then fits OLS models for every combination of scheme, form,
and covariate set requested.

\strong{Built-in covariate sets} (using ACS 2022 variables from the crosswalk):
\itemize{
\item \code{"minimal"}: log(population) + log(median income)
\item \code{"full"}: minimal + percent BA or higher + percent non-Hispanic white
\item \code{"state_fe"}: full + state fixed effects (\code{factor(state_fips)})
}

\strong{Ordinal predictors} are standardized (mean 0, SD 1) so that
coefficients are comparable across schemes with different scale lengths
(RUCC: 1-9, RUCA: 1-10, NCHS: 1-6, OMB: 1-3).

\strong{Binary predictors} code metropolitan = 0, nonmetropolitan = 1, using
standard cutpoints: RUCC >= 4, RUCA >= 4, NCHS >= 5, OMB != "metro".

Any user-supplied \code{covariates} are appended to all three built-in covariate
sets.
}
\examples{
\dontrun{
# Minimal example with a built-in outcome proxy
library(dplyr)

# Attach a synthetic outcome to the crosswalk
df <- county_rurality |>
  select(fips) |>
  mutate(y = rnorm(n()))

results <- rurality_spec(df, outcome = "y")
print(results)
}

}
